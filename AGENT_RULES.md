# Regras de Segurança – Resumo Rápido

## 🚫 NUNCA Deletar
- `.agents/` `.git/` `.project/` `supabase/migrations/` `alembic/` `tests/` `worker/` `.vscode/`

## ⚠️ CUIDADO ao Editar
- `.env` `.env.example` `.mcp.json` `docker-compose.yml` `requirements.txt` `main.py`

## ✅ Seguro Criar/Editar
- `app/` `worker/` `tests/` `docs/` (exceto estruturas base)

---
Ver [.agents/AGENTS.md](.agents/AGENTS.md) para detalhes completos.
