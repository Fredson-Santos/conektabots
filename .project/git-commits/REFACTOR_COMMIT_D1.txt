feat: Refactor Bots Management page to modern SaaS design

DESCRIPTION:
Complete redesign of Bots Management interface to match modern SaaS standards 
(Stripe/Linear/Vercel style). Transformed from generic UI to professional, 
minimalist dashboard with clean typography, consistent spacing, and Heroicons.

D1.1 - Page Layout Refactoring (page.tsx):
✅ Added statistics cards at top:
   - Total Bots card (blue accent)
   - Active Bots card (green accent)
   - Inactive Bots card (gray accent)
✅ Professional visual hierarchy with 8px grid spacing
   - Page title with description
   - Stats cards in responsive 3-column grid
   - Filters and table sections
✅ Typography hierarchy:
   - Page title: text-base gray with 8px grid spacing
   - Stats labels: uppercase, text-xs font-medium
   - Stats values: text-2xl font-semibold
✅ Responsive layout: Mobile-first grid collapse on sm/md/lg breakpoints
✅ Spacing: space-y-8 between major sections

D1.2 - BotsTable Component Refactoring:
✅ Replaced ALL emojis with Heroicons:
   - CheckCircleIcon (green) for active status
   - XCircleIcon (gray) for inactive status
   - SparklesIcon for empty state
   - PencilIcon for edit action
   - TrashIcon for delete action
✅ Professional table styling:
   - Header: bg-gray-50 with uppercase text-xs labels
   - Rows: hover:bg-gray-50 smooth transition
   - No alternating row colors (modern flat design)
   - Clean borders only, no shadows
✅ Responsive mobile cards (< 768px):
   - Each bot rendered as card with name/API ID at top
   - Status icon in top-right corner
   - Edit/Delete buttons below with icon + text
✅ Improved loading state:
   - 3 placeholder rows with opacity-60 fade
   - Removed bounce animation (subtle is better)
   - Gray placeholder backgrounds
✅ Professional empty state:
   - SparklesIcon in center
   - "No bots yet" heading with description
   - Proper visual hierarchy
✅ Clean pagination:
   - Text links instead of arrow buttons
   - Disabled state with proper opacity
   - Removed arrow emoji (← →) → text labels

D1.3 - BotForm Component Refactoring:
✅ Professional form layout with clear visual structure
   - Field grouping: space-y-6 for main sections
   - Labels above inputs with red asterisk for required
   - Helper text under each field in small gray
✅ Enhanced input styling:
   - Larger padding (px-4 py-2)
   - Better focus states: ring-blue-400 on normal, ring-red-400 on error
   - Error styling: border-red-300 + bg-red-50
   - Placeholder text in neutral gray
✅ Field descriptions with helpful context:
   - Bot Name: "Enter a unique name for your bot (2-50 characters)"
   - API ID: Link to https://my.telegram.org with visual link styling
   - API Hash: Link to https://my.telegram.org
   - Phone: "Phone number associated with this Telegram account"
✅ Better error handling:
   - Inline error messages (no alerts)
   - Field-level validation feedback
   - Touched state to avoid premature errors
   - Submit error alert with title + description
✅ Improved button styling:
   - Primary button: bg-blue-600 with hover/active states
   - Cancel button: bg-gray-200 with proper contrast
   - Loading spinner inside button
✅ Proper spacing: 8px grid throughout

D1.4 - Modal Components Refactoring:

CreateBotModal.tsx:
✅ Modern modal design:
   - Backdrop: bg-black bg-opacity-50 (no blur for performance)
   - Position: Centered with proper padding on mobile
   - Max-width: 600px for form readability
✅ Professional header:
   - Title + close button (XMarkIcon from Heroicons)
   - Sticky header on scroll
   - Proper spacing (px-6 py-4)
✅ Focus management:
   - Auto-focus first input on open
   - ESC key closes modal
   - Body overflow hidden while open
   - Focus trap (keyboard navigation stays in modal)
✅ Accessibility:
   - ARIA roles: dialog, modal, labelledby, describedby
   - Proper heading hierarchy
   - Semantic structure

DeleteConfirmationModal.tsx (Danger Confirmation):
✅ Professional danger confirmation design:
   - Red-themed header with ExclamationTriangleIcon
   - Visual hierarchy: Icon → Title → Description
   - Warning box with red accent
✅ Danger color scheme:
   - Header: bg-red-50 with red icon circle
   - Delete button: bg-red-600 hover:bg-red-700
   - Warning text in red tones
✅ Clear messaging:
   - Title: "Delete Bot?"
   - Description explains consequence: "This action cannot be undone..."
   - Bot name emphasized in quotes
   - Additional warning box below
✅ Accessibility & Safety:
   - ARIA role: alertdialog
   - ESC key closes modal
   - Cancel button is primary interaction pattern
   - Delete button positioned to prevent accidental clicks
   - Proper color contrast (red #EF4444 on white)

DESIGN SYSTEM APPLIED:
✅ Color Palette:
   - Primary Blue: #2563EB (buttons, active states)
   - Success Green: #10B981 (active status)
   - Danger Red: #EF4444 (delete actions)
   - Neutral Grays: #F9FAFB to #111827 scale
✅ Typography:
   - Font: Inter system stack (default in Tailwind)
   - Hierarchy: text-2xl → text-lg → text-sm → text-xs
   - Weights: semibold (600) for emphasis, medium (500) for labels
✅ Spacing:
   - 8px grid system throughout
   - Between sections: space-y-8 (24px)
   - Between items: space-y-6, space-y-4, space-y-2
   - Padding inside cards: px-6 py-4 (24px)
✅ Components:
   - All borders: 1px solid border-gray-200
   - Rounded corners: rounded-lg (8px) for consistency
   - Hover states: transition-colors on interactive elements
   - No drop shadows (flat design preference)
✅ Responsiveness:
   - Mobile-first approach
   - Hidden elements: hidden md:block (table)
   - Grid collapse: grid-cols-1 md:grid-cols-3
   - Card layout on mobile, table on desktop

TESTING COMPLETED:
✅ All buttons functional (create, edit, delete, pagination)
✅ Form validation displays inline without alerts
✅ Modals open/close properly with ESC key and backdrop
✅ Responsive behavior verified: 320px, 768px, 1200px
✅ Keyboard navigation works (Tab, Enter, ESC)
✅ Status indicators update correctly (CheckCircle/XCircle icons)
✅ Empty state displays with proper messaging
✅ Loading state shows subtle animation
✅ No console errors
✅ No emojis anywhere in interface

FILES MODIFIED:
- frontend/app/(dashboard)/bots/page.tsx
- frontend/app/(dashboard)/bots/components/BotsTable.tsx
- frontend/app/(dashboard)/bots/components/BotForm.tsx
- frontend/app/(dashboard)/bots/components/CreateBotModal.tsx
- frontend/app/(dashboard)/bots/components/DeleteConfirmationModal.tsx

VISUAL IMPROVEMENTS:
~ From: Generic dashboard with emoji, inconsistent spacing, basic styling
~ To: Modern SaaS interface with professional typography, consistent 8px grid,
     Heroicons, clean flat design, responsive layouts

FUNCTIONALITY PRESERVED:
✅ All business logic unchanged
✅ API calls work identically
✅ State management untouched
✅ Pagination logic preserved
✅ Validation rules preserved

ACCESSIBILITY:
✅ WCAG AA contrast ratios (4.5:1+ for text)
✅ Semantic HTML structure
✅ Proper ARIA roles and labels
✅ Keyboard navigation support
✅ Focus indicators visible
✅ Error messages associated with inputs

PERFORMANCE:
✅ No new dependencies added
✅ Removed unnecessary animations (shadows vs performance)
✅ Improved rendering with proper CSS classes
✅ Minimal bundle size impact (Heroicons already in project)

BACKWARDS COMPATIBILITY:
✅ No breaking changes to component props
✅ API contract unchanged
✅ No database migrations needed
✅ Works with existing authentication/authorization
