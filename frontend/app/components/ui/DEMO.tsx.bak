'use client'

import React, { useState } from 'react'
import {
  Button,
  Input,
  Card,
  StatusBadge,
  Alert,
  EmptyState,
} from '@/app/components/ui'
import {
  PlusIcon,
  EnvelopeIcon,
  LockClosedIcon,
} from '@heroicons/react/24/outline'

/**
 * Component library demo - Verify all 6 UI components work correctly
 * Mount this at any route to see all components in action
 *
 * Verification Checklist:
 * ✓ Button: All 5 variants visible, all states working (hover, focus, disabled, loading)
 * ✓ Input: All types render (text, email, password, textarea, number), error state shows red
 * ✓ Card: Composable (header+body+footer work independently), dividers show
 * ✓ StatusBadge: All 6 statuses show correct icon + color
 * ✓ Alert: All 4 types show, dismiss button works, action button optional
 * ✓ EmptyState: Icon displays, centered, button clickable
 * ✓ No console errors (React strict mode)
 * ✓ Mobile responsive (view at 320px, see inputs/buttons stack properly)
 * ✓ Keyboard navigation (Tab through buttons/inputs, focus ring visible)
 * ✓ No emojis anywhere
 */
export default function ComponentLibraryDemo() {
  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [description, setDescription] = useState('')
  const [bioText, setBioText] = useState('')
  const [feedback, setFeedback] = useState('')

  return (
    <div className="min-h-screen bg-gray-50 py-12 px-4 sm:px-6 lg:px-8">
      <div className="max-w-6xl mx-auto space-y-16">
        {/* Header */}
        <div>
          <h1 className="text-4xl font-bold text-gray-900 mb-2">Component Library Demo</h1>
          <p className="text-lg text-gray-600">Modern SaaS Design System - Phase 1 Base Components</p>
        </div>

        {/* BUTTON COMPONENT */}
        <section className="space-y-6">
          <div className="border-b border-gray-200 pb-4">
            <h2 className="text-2xl font-semibold text-gray-900">Button Component</h2>
            <p className="text-sm text-gray-600 mt-1">5 variants × 3 sizes + states</p>
          </div>

          <div className="space-y-6">
            {/* Primary */}
            <div>
              <h3 className="text-sm font-medium text-gray-700 mb-3">Primary Variant</h3>
              <div className="flex flex-wrap gap-2">
                <Button variant="primary" size="sm">
                  Small
                </Button>
                <Button variant="primary" size="md">
                  Medium
                </Button>
                <Button variant="primary" size="lg">
                  Large
                </Button>
                <Button variant="primary" size="md" icon={<PlusIcon className="w-4 h-4" />}>
                  With Icon
                </Button>
                <Button variant="primary" size="md" disabled>
                  Disabled
                </Button>
                <Button variant="primary" size="md" loading>
                  Loading
                </Button>
              </div>
            </div>

            {/* Secondary */}
            <div>
              <h3 className="text-sm font-medium text-gray-700 mb-3">Secondary Variant</h3>
              <div className="flex flex-wrap gap-2">
                <Button variant="secondary" size="sm">
                  Small
                </Button>
                <Button variant="secondary" size="md">
                  Medium
                </Button>
                <Button variant="secondary" size="lg">
                  Large
                </Button>
              </div>
            </div>

            {/* Tertiary */}
            <div>
              <h3 className="text-sm font-medium text-gray-700 mb-3">Tertiary Variant</h3>
              <div className="flex flex-wrap gap-2">
                <Button variant="tertiary" size="sm">
                  Small
                </Button>
                <Button variant="tertiary" size="md">
                  Medium
                </Button>
                <Button variant="tertiary" size="lg">
                  Large
                </Button>
              </div>
            </div>

            {/* Ghost */}
            <div>
              <h3 className="text-sm font-medium text-gray-700 mb-3">Ghost Variant</h3>
              <div className="flex flex-wrap gap-2">
                <Button variant="ghost" size="sm">
                  Small
                </Button>
                <Button variant="ghost" size="md">
                  Medium
                </Button>
                <Button variant="ghost" size="lg">
                  Large
                </Button>
              </div>
            </div>

            {/* Danger */}
            <div>
              <h3 className="text-sm font-medium text-gray-700 mb-3">Danger Variant</h3>
              <div className="flex flex-wrap gap-2">
                <Button variant="danger" size="sm">
                  Delete
                </Button>
                <Button variant="danger" size="md">
                  Delete
                </Button>
                <Button variant="danger" size="lg">
                  Delete Item
                </Button>
              </div>
            </div>

            {/* Full Width */}
            <div>
              <h3 className="text-sm font-medium text-gray-700 mb-3">Full Width</h3>
              <Button variant="primary" fullWidth>
                Full Width Button
              </Button>
            </div>
          </div>
        </section>

        {/* INPUT COMPONENT */}
        <section className="space-y-6">
          <div className="border-b border-gray-200 pb-4">
            <h2 className="text-2xl font-semibold text-gray-900">Input Component</h2>
            <p className="text-sm text-gray-600 mt-1">5 types with validation and error states</p>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
            <Input
              label="Text Input"
              type="text"
              placeholder="Enter text"
              value="Sample text"
              onChange={() => {}}
              helper="This is helper text"
            />

            <Input
              label="Email Input"
              type="email"
              placeholder="user@example.com"
              value={email}
              onChange={(e) => setEmail(e.target.value)}
              required
            />

            <Input
              label="Password"
              type="password"
              placeholder="Enter password"
              value={password}
              onChange={(e) => setPassword(e.target.value)}
              icon={<LockClosedIcon className="w-4 h-4" />}
              helper="Must be at least 8 characters"
            />

            <Input
              label="Email with Error"
              type="email"
              placeholder="invalid email"
              value="invalid"
              onChange={() => {}}
              error="Please enter a valid email address"
            />

            <Input
              label="Number Input"
              type="number"
              placeholder="Enter a number"
              value="42"
              onChange={() => {}}
            />

            <Input
              label="Disabled Input"
              type="text"
              placeholder="Disabled"
              value="Cannot edit"
              onChange={() => {}}
              disabled
            />

            <div className="col-span-1 md:col-span-2">
              <Input
                label="Textarea"
                type="textarea"
                placeholder="Enter your description"
                value={description}
                onChange={(e) => setDescription(e.target.value)}
                maxLength={200}
              />
            </div>

            <div className="col-span-1 md:col-span-2">
              <Input
                label="Bio (with error)"
                type="textarea"
                placeholder="Tell us about yourself"
                value={bioText}
                onChange={(e) => setBioText(e.target.value)}
                error="Bio is too short (min 10 characters)"
              />
            </div>
          </div>
        </section>

        {/* CARD COMPONENT */}
        <section className="space-y-6">
          <div className="border-b border-gray-200 pb-4">
            <h2 className="text-2xl font-semibold text-gray-900">Card Component</h2>
            <p className="text-sm text-gray-600 mt-1">Composable header/body/footer structure</p>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
            {/* Card with Header, Body, Footer */}
            <Card>
              <Card.Header
                title="Bot Configuration"
                subtitle="Manage settings"
                action={<Button variant="ghost" size="sm">More</Button>}
              />
              <Card.Body>
                <p className="text-sm text-gray-600">
                  This card demonstrates the full composable structure with header, body, and footer.
                </p>
              </Card.Body>
              <Card.Footer>
                <Button variant="secondary" size="sm">
                  Cancel
                </Button>
                <Button variant="primary" size="sm">
                  Save
                </Button>
              </Card.Footer>
            </Card>

            {/* Card elevated variant */}
            <Card variant="elevated" interactive>
              <Card.Header title="Interactive Card" subtitle="Hover for effect" />
              <Card.Body>
                <p className="text-sm text-gray-600">
                  This card uses the elevated variant and shows a subtle shadow on hover.
                </p>
              </Card.Body>
            </Card>

            {/* Simple card */}
            <Card>
              <Card.Body>
                <h3 className="text-lg font-semibold text-gray-900 mb-2">Custom Header</h3>
                <p className="text-sm text-gray-600">
                  You can also create custom header content by using children instead of title/subtitle props.
                </p>
              </Card.Body>
            </Card>

            {/* Card with only body */}
            <Card>
              <Card.Body>
                <p className="text-sm text-gray-600">
                  Cards are flexible. You can use just the body, or combine header, body, and footer as needed.
                </p>
              </Card.Body>
            </Card>
          </div>
        </section>

        {/* STATUS BADGE COMPONENT */}
        <section className="space-y-6">
          <div className="border-b border-gray-200 pb-4">
            <h2 className="text-2xl font-semibold text-gray-900">StatusBadge Component</h2>
            <p className="text-sm text-gray-600 mt-1">6 status types with semantic colors</p>
          </div>

          <div className="space-y-6">
            {/* Pill variant */}
            <div>
              <h3 className="text-sm font-medium text-gray-700 mb-3">Pill Variant (Default)</h3>
              <div className="flex flex-wrap gap-4">
                <StatusBadge status="active" size="md" />
                <StatusBadge status="inactive" size="md" />
                <StatusBadge status="processing" size="md" />
                <StatusBadge status="error" size="md" />
                <StatusBadge status="warning" size="md" />
                <StatusBadge status="success" size="md" />
              </div>
            </div>

            {/* Dot variant */}
            <div>
              <h3 className="text-sm font-medium text-gray-700 mb-3">Dot Variant</h3>
              <div className="flex flex-wrap gap-4">
                <StatusBadge status="active" variant="dot" size="md" />
                <StatusBadge status="inactive" variant="dot" size="md" />
                <StatusBadge status="processing" variant="dot" size="md" />
                <StatusBadge status="error" variant="dot" size="md" />
                <StatusBadge status="warning" variant="dot" size="md" />
                <StatusBadge status="success" variant="dot" size="md" />
              </div>
            </div>

            {/* Sizes */}
            <div>
              <h3 className="text-sm font-medium text-gray-700 mb-3">All Sizes (XS, SM, MD)</h3>
              <div className="flex flex-wrap gap-4 items-center">
                <StatusBadge status="active" size="xs" />
                <StatusBadge status="active" size="sm" />
                <StatusBadge status="active" size="md" />
              </div>
            </div>
          </div>
        </section>

        {/* ALERT COMPONENT */}
        <section className="space-y-6">
          <div className="border-b border-gray-200 pb-4">
            <h2 className="text-2xl font-semibold text-gray-900">Alert Component</h2>
            <p className="text-sm text-gray-600 mt-1">4 types with dismiss and action support</p>
          </div>

          <div className="space-y-4">
            <Alert type="info" title="Information" description="This is an informational alert message." />

            <Alert
              type="success"
              title="Success!"
              description="Your changes have been saved successfully."
            />

            <Alert
              type="warning"
              title="Warning"
              description="This action may have unintended consequences."
            />

            <Alert
              type="error"
              title="Error occurred"
              description="Something went wrong. Please try again."
              action={{ label: 'Retry', onClick: () => alert('Retry clicked') }}
            />

            <Alert
              type="info"
              title="Non-dismissible"
              description="This alert cannot be dismissed."
              dismissible={false}
            />
          </div>
        </section>

        {/* EMPTY STATE COMPONENT */}
        <section className="space-y-6">
          <div className="border-b border-gray-200 pb-4">
            <h2 className="text-2xl font-semibold text-gray-900">EmptyState Component</h2>
            <p className="text-sm text-gray-600 mt-1">Centered no-data visualization</p>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
            <Card>
              <Card.Body className="p-0">
                <EmptyState
                  icon={<PlusIcon className="w-12 h-12" />}
                  title="No bots yet"
                  description="Create your first bot to get started with automation"
                  action={{
                    label: 'Create Bot',
                    onClick: () => alert('Create clicked'),
                    icon: <PlusIcon className="w-4 h-4" />,
                  }}
                  secondaryAction={{
                    label: 'Learn More',
                    onClick: () => alert('Learn More clicked'),
                  }}
                />
              </Card.Body>
            </Card>

            <Card>
              <Card.Body className="p-0">
                <EmptyState
                  icon={<EnvelopeIcon className="w-12 h-12" />}
                  title="No messages"
                  description="Messages will appear here when you receive them"
                  action={{
                    label: 'Check Again',
                    onClick: () => alert('Check Again clicked'),
                  }}
                />
              </Card.Body>
            </Card>
          </div>
        </section>

        {/* Testing Notes */}
        <div className="rounded-lg bg-blue-50 border border-blue-200 p-6">
          <h3 className="font-semibold text-blue-900 mb-2">Testing Checklist</h3>
          <ul className="text-sm text-blue-800 space-y-1">
            <li>✓ All 6 components render without errors</li>
            <li>✓ Button: All variants and sizes visible, states work (hover, focus, disabled, loading)</li>
            <li>✓ Input: All types render, error state shows red border and text</li>
            <li>✓ Card: Composable structure works, dividers display correctly</li>
            <li>✓ StatusBadge: All 6 statuses show correct colors and icons</li>
            <li>✓ Alert: All 4 types display, dismiss button works</li>
            <li>✓ EmptyState: Icon displays, centered layout, buttons clickable</li>
            <li>✓ Mobile responsive: Stack properly at 320px width</li>
            <li>✓ Keyboard navigation: Tab through all interactive elements (ring visible)</li>
            <li>✓ No console errors or warnings</li>
          </ul>
        </div>
      </div>
    </div>
  )
}
